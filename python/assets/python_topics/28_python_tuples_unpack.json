{
  "topic": "tuples unpack",
  "sections": [
    {
      "heading": "Unpacking a Tuple",
      "description": "When we create a tuple, we normally assign values to it. This is called \"packing\" a tuple: But, in Python, we are also allowed to extract the values back into variables. This is called \"unpacking\":",
      "code_blocks": [
        {
          "example_title": "Example",
          "notes": "Packing a tuple:",
          "raw_html_class": "w3-code notranslate pythonHigh",
          "code": "fruits = (\"apple\", \"banana\", \"cherry\")"
        },
        {
          "example_title": "Example",
          "notes": "Unpacking a tuple:",
          "raw_html_class": "w3-code notranslate pythonHigh",
          "code": "fruits = (\"apple\", \"banana\", \"cherry\")\n\n\n\n(green, yellow, red) = fruits\n\n\n\nprint(green)\n\nprint(yellow)\n\nprint(red)"
        }
      ]
    },
    {
      "heading": "Using Asterisk*",
      "description": "If the number of variables is less than the number of values, you can add an * to the variable name and the values will be assigned to the variable as a list: If the asterisk is added to another variable name than the last, Python will assign values to the variable until the number of values left matches the number of variables left.",
      "code_blocks": [
        {
          "example_title": "Example",
          "notes": "Assign the rest of the values as a list called \"red\":",
          "raw_html_class": "w3-code notranslate pythonHigh",
          "code": "fruits = (\"apple\", \"banana\", \"cherry\", \"strawberry\", \"raspberry\")\n\n\n\n(green, yellow, *red) = fruits\n\n\n\nprint(green)\n\nprint(yellow)\n\nprint(red)"
        },
        {
          "example_title": "Example",
          "notes": "Add a list of values the \"tropic\" variable:",
          "raw_html_class": "w3-code notranslate pythonHigh",
          "code": "fruits = (\"apple\", \"mango\", \"papaya\", \"pineapple\", \"cherry\")\n\n\n\n(green, *tropic, red) = fruits\n\n\n\nprint(green)\n\nprint(tropic)\n\nprint(red)"
        }
      ]
    }
  ]
}