{
  "title": "C++ The foreach Loop",
  "content": [
    {
      "heading": "The foreach Loop",
      "paragraphs": [
        "There is also a \"for-each loop\" (also known as ranged-based for loop), which is \nused exclusively to loop through elements in an array (or other data structures):",
        "The following example outputs all elements in an array, using a \"for-each loop\":"
      ]
    },
    {
      "heading": "Syntax",
      "paragraphs": [
        "for (type variableName : arrayName) { \n // code block to be executed\n  }"
      ]
    },
    {
      "heading": "Example",
      "paragraphs": [
        "int myNumbers[5] = {10, 20, 30, 40, 50} ;\nfor (int i : myNumbers) {  \n  cout << i << \"\\n\" ;\n}"
      ]
    }
  ]
}